# 🛠 My Personal Scripts

A collection of my personal working scripts, with documentation explaining what each script does.

## 📂 Structure

- `src/` — Contains all executable scripts.
- `docs/` — Markdown documentation explaining each script.
- `README.md` — Overview of the repository.

## ⚡ Example

- `ccwd.sh` — Copies your **current working directory** to multiple buffers (Main & X11).
- `backup.sh` — Automates project backups to a specified folder.

## 📝 Usage

1. Navigate to the `src/` folder:
   ```bash
   cd scripts/
